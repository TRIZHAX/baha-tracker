export default function Loading() {
  return <main className="flex min-h-screen items-center justify-center bg-[hsl(var(--background))]"><div className="flex items-center gap-3 font-semibold"><span className="h-3 w-3 animate-pulse rounded-full bg-cyan-600" />Loading live flood map</div></main>
}
